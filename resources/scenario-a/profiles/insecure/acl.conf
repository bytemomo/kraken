# Allow ALL users (including anonymous) to read and write to ALL topics
topic readwrite #

# Allow everyone to read $SYS topics (system information disclosure)
topic read $SYS/#

# Pattern-based wildcard - matches everything for all users
pattern readwrite #
